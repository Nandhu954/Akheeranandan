<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center">

    <!-- Title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🔐 Your Owner Verification QR"
        android:textColor="#1B3A6B"
        android:textSize="18sp"
        android:textStyle="bold"
        android:gravity="center" />

    <!-- Item name -->
    <TextView
        android:id="@+id/tvOwnerQRItemName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="Lost Item"
        android:textColor="#F47920"
        android:textSize="14sp"
        android:textStyle="bold"
        android:gravity="center" />

    <!-- Instruction -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="16dp"
        android:text="Show this QR code to the finder when collecting your item. The finder will scan it to confirm you are the rightful owner before handing over."
        android:textColor="#6C757D"
        android:textSize="13sp"
        android:gravity="center"
        android:lineSpacingMultiplier="1.3" />

    <!-- QR Code Image -->
    <ImageView
        android:id="@+id/ivOwnerQR"
        android:layout_width="220dp"
        android:layout_height="220dp"
        android:scaleType="fitCenter"
        android:contentDescription="Owner verification QR code"
        android:background="#F4F6FA"
        android:padding="8dp" />

    <!-- Code text -->
    <TextView
        android:id="@+id/tvOwnerCode"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="CF-XXXXXX"
        android:textColor="#1B3A6B"
        android:textSize="28sp"
        android:textStyle="bold"
        android:letterSpacing="0.1"
        android:fontFamily="monospace" />

    <!-- Warning -->
    <com.google.android.material.card.MaterialCardView
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        app:cardCornerRadius="10dp"
        app:cardElevation="0dp"
        app:strokeWidth="1dp"
        app:strokeColor="#F47920"
        app:cardBackgroundColor="#FFF6EE">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="12dp"
            android:gravity="center_vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="⚠️"
                android:textSize="16sp" />

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:text="Save this code! You can view it again in My Reports → Item Detail."
                android:textColor="#D4650D"
                android:textSize="12sp" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

</LinearLayout>
